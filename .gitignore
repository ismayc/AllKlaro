# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# TLS certificates for phone/LAN mode (contain a private key — never commit)
certs/

# Personal conversation transcripts used for local testing
testing/

# Saved translation corrections (personal conversation content)
corrections.jsonl

# Local investigation write-ups. They live under docs/ for convenience, but
# docs/ is the public GitHub Pages source — committing them would publish them.
docs/findings/

# Pipeline traces (see the README's "When it falls behind" section)
allklaro-trace.jsonl

# macOS
.DS_Store

# Coverage runs (uv run --with pytest-cov pytest --cov=server --cov=voiceprint)
.coverage
htmlcov/
